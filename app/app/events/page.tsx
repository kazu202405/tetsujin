// ============================================================
// /app/events は「会を探す」(/app/post) に統合済み
// ============================================================
// 元は同じ機能のmock画面がここにもあり、どこからもリンクされていなかった。
// 削除するとブックマークが404になるため、リダイレクトだけ残す。
// ============================================================
import { redirect } from "next/navigation";

export default function EventsPage() {
  redirect("/app/post");
}
